# LLM App API Contracts

Author-owned app-api contract inputs for the `intelligence/llm` capability.

Materialized OpenAPI authority files live under `apis/app-api/` and `sdks/sdkwork-llm-app-sdk/openapi/`.
