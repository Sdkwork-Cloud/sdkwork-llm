# LLM Open API Contracts

Author-owned open-api contract inputs for the `intelligence/llm` capability.

Materialized OpenAPI authority files live under `apis/open-api/` and `sdks/sdkwork-llm-sdk/openapi/`.
