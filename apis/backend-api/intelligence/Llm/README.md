# LLM Backend API Contracts

Author-owned backend-api contract inputs for the `intelligence/llm` capability.

Materialized OpenAPI authority files live under `apis/backend-api/` and `sdks/sdkwork-llm-backend-sdk/openapi/`.
