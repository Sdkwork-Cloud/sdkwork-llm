apiVersion: v1
kind: Service
metadata:
  name: sdkwork-llm-api-server
  labels:
    app.kubernetes.io/name: sdkwork-llm
    app.kubernetes.io/component: api-server
    app.kubernetes.io/part-of: sdkwork-llm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sdkwork-llm
    app.kubernetes.io/component: api-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
